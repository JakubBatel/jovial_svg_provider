name: jovial_svg_provider
description: A new Flutter package project.
version: 0.0.1

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: ">=3.10.0"

dependencies:
  equatable: ^2.0.5
  flutter:
    sdk: flutter
  jovial_svg: ^1.1.21

dev_dependencies:
  flutter_lints: ^4.0.0
  flutter_test:
    sdk: flutter

